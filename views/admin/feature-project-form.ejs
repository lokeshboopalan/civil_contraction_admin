<%- include('../layouts/admin-users', { 
    title: isEdit ? 'Edit Feature Project' : 'Create Feature Project',
    currentPage: 'feature-projects',
    user: typeof user !== 'undefined' ? user : null,
    body: include('../partials/feature-project-form-content', { 
        isEdit: isEdit,
        projectData: projectData,
        error: typeof error !== 'undefined' ? error : null
    })
}) %>